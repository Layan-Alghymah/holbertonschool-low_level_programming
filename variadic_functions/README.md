# C - Variadic Functions

This project demonstrates how to create and use variadic functions in C with the `<stdarg.h>` library, using `va_list`, `va_start`, `va_arg`, and `va_end` to handle a variable number of arguments.
